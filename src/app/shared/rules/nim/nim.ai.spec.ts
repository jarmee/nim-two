import { calculatePlay } from "./nim.ai";

describe("NimAi", () => {
  describe("calculatePlay", () => {
    it("should return something", () => {
      expect(calculatePlay).toBeTruthy();
    });
  });
});
